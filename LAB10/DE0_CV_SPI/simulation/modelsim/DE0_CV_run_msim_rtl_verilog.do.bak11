transcript on
if {[file exists rtl_work]} {
	vdel -lib rtl_work -all
}
vlib rtl_work
vmap work rtl_work

vlog -sv -work work +incdir+D:/Quartus/code/DE0_CV_SPI/design {D:/Quartus/code/DE0_CV_SPI/design/DE0_CV.sv}

